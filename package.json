{
  "name": "how-sitter",
  "version": "1.0.0",
  "description": "House sitting platform",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build"
  },
  "dependencies": {
    "@netlify/functions": "^5.1.2",
    "@netlify/neon": "^0.1.2"
  }
}
